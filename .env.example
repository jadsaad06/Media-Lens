# === Core Services ===
# DATABASE_URL: SQLAlchemy URL for Postgres (psycopg3 driver recommended)
# Example: postgresql+psycopg://medialens:medialens@localhost:5432/medialens
DATABASE_URL=postgresql+psycopg://medialens:medialens@localhost:5432/medialens

# RSS_FEEDS: Comma-separated RSS feed URLs for ingestion
RSS_FEEDS=https://rss.nytimes.com/services/xml/rss/nyt/HomePage.xml,https://feeds.bbci.co.uk/news/rss.xml

# NEWSAPI_KEY: Optional, used for future data augmentation
NEWSAPI_KEY=

# DATA_DIR: Where embed_titles/cluster_events read/write arrays
DATA_DIR=./data


# === Evaluation: Global ===
# SEED: Reproducibility seed
SEED=42
# EVAL_EMBEDDER: Sentence-Transformers model (e.g., all-MiniLM-L6-v2 or all-mpnet-base-v2)
EVAL_EMBEDDER=sentence-transformers/all-MiniLM-L6-v2
# EVAL_DATA_DIR: Where evaluation datasets (e.g., FNC-1 CSVs) are stored
EVAL_DATA_DIR=./data
# EVAL_OUT_DIR: Where persisted models/artifacts are saved
EVAL_OUT_DIR=./outputs
# REPORTS_DIR: Where human-readable reports/plots are written
REPORTS_DIR=./reports


# === Stance Controls ===
# STANCE_CALIBRATE: 1 calibrates LinearSVC to get predict_proba; 0 skips for speed
STANCE_CALIBRATE=1


# === Framing Controls ===
# FRAMING_TUNE_THRESHOLDS: 1 tunes per-tag thresholds on validation; 0 uses a global threshold
FRAMING_TUNE_THRESHOLDS=1
# FRAMING_THRESHOLD: Global threshold when tuning is disabled
FRAMING_THRESHOLD=0.3
# FRAMING_EMBEDDINGS_ONLY: 1 uses SBERT-only; 0 uses TF-IDF (or ensemble if enabled)
FRAMING_EMBEDDINGS_ONLY=0
# FRAMING_ENSEMBLE: 1 averages TF-IDF and SBERT probabilities
FRAMING_ENSEMBLE=0
# FRAMING_TOPK: Force top-k tags per sample after thresholding (0 disables)
FRAMING_TOPK=0
# FRAMING_TFIDF_NGRAMS: TF-IDF n-gram range, format: min,max
FRAMING_TFIDF_NGRAMS=1,2
# FRAMING_TFIDF_MAX_FEATURES: Max TF-IDF vocabulary size
FRAMING_TFIDF_MAX_FEATURES=20000
